cd "file location"
git config --global user.email nishalnthingalaya1@gmail.com
git config --global user.name NishalNT
git config --list  -->to view the details
git init --> new repo
git clone url -->clone the repo
git init -b main --> to convert the master to main
git branch --move master main
git status
git add <filename> --> adds a file
git commit -m ""
git log --> see all the commmit
git reset filename --> reset the recent commit
git revert --> discard the current commit
git diff -->step up step of what u did like add or delete a file
git diff --cached -->
git commit --amend
shift + ':' + q + enter
or escape :qa!